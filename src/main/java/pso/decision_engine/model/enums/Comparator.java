package pso.decision_engine.model.enums;

public enum Comparator {
	EQUAL, NOT_EQUAL, SMALLER, GREATER, 
	SMALLER_OR_EQUAL, SMALLER_OR_GREATER,
	BETWEEN, IN_LIST, NOT_IN_LIST, 
	STARTS_WITH, CONTAINS, ENDS_WITH
}
